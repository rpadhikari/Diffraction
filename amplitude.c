double sin(double);
// angles are in radian here (will be converted to degree in
// main.c code
double amplitude(double a, double theta, double lambda) {
  double pi=3.14159;
  double alpha=pi*a*sin(theta)/lambda;
  return a*sin(alpha)/sin(alpha/(n*lambda));
}

